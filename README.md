# *httk* ada miniparser and optimadeparser

This repository contains the httk miniparser.py translated with the help of AI to the Ada programming language. 
An optimadeparser implementation is also included, which uses miniparser to parse an OPTIMADE filter string.

# Build

To build all binaries
```
make 
```

To run the tests
```
make tests
```

